# 待确认的数据问题（来自三遍全文审查）

## 1. 表 4.5（NBD 泛化表）BiLSTM₆₀ 行疑似复制错误

该行数值与 LightGBM 行完全相同：0.041 / 0.023 / 0.025 / 0.095 / 0.046。
正文还写了 "BiLSTM₆₀ achieves the same average MAE as LightGBM (0.046)"，疑似为错误数据打的圆场。

**需要**：查代码/日志，给出 BiLSTM₆₀ 在 NBD 上 wlen={10,35,55,75} 的真实 MAE。

## 2. Flash% 与 KB 数矛盾

- 表 4.4（部署表）：Flash% 2.71% (NBD) / 2.23% (NRD)
- 但实际模型大小 119.5 KB / 126.05 KB ÷ 4 MB = **2.92% / 3.08%**
- 百分比不仅数值对不上，连大小顺序都反了（NRD 模型更大，百分比却更小）

**需要**：确认 Flash% 的口径——是只算模型参数还是模型+固件+HAL？真实值是多少？

## 3. "under 3%" 表述不成立

NRD 模型 126.05 KB = 3.08%，超过 3%。
conclusion 和 summary 中 "under 3%" 的说法需要改成 "≈3%" 或修数字。

## 4. RAM 百分比与 KB 数矛盾

正文写 "0.49% of RAM (≈1.6 KB)"，但 0.49% × 384 KB = 1.88 KB。

**需要**：确认 RAM 真实占用 KB 数和百分比。

## 5. NBD 最优 LR 超出搜索范围

表 4.2 写 NBD 最优 LR = 0.147，但 Ch3 表 3.2 的搜索范围是 [10⁻⁵, 10⁻¹]（上限 0.1）。
0.147 > 0.1，必有一个错。

**需要**：确认真实的搜索范围或最优 LR 值。
