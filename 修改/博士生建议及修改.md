For unseen NBD window lengths, Table 4.5 reports: The entire BiLSTM60 row is exactly identical to the LightGBM row, including all four values and the average, also the paper have been published with these wrong info, if the experiment itself is still available and easy to run, I would check the real result!
Table 4.1 says that the NBD “SoH label” ranges from 1.29–1.86 Ah, whereas NRD labels are 41.6–100%. The text then explains that the two MAEs are simply on different scales. Technically, an Ah value is capacity, check this one too!!!
Table 4.4 add the metric MAE [%], MSE [%], Cycles [kcycles] and remove "k" from the tables, and the "M" become "k", Total Flash [%] and RAM [%]
Also watch out for one thing in general so you do not mention it during the presentation “LSTMs cannot process variable-length inputs.” it is honestly support variable length but the deployed BiLSTM baseline and its SPC5Studio AI conversion flow use a fixed input tensor shape, also that "LightGBM provides input-length flexibility", it is not by itself doing that, but through feature extraction you can do it for any machine learning model not only lightgbm
Also when you mentioned Energy stated at "Estimated energy per inference [µJ]" in general when report a result related to it since we did not measure it in reality
In the summary section, I see at the end of the page "production automotive hardware with little need for updates", better say something like "demonstrates or proved the feasibility of lightweight SoH inference on an automotive-grade microcontroller reduces sensitivity to observation-window length, potentially lowering the need for retraining caused only or solely by changes in input-window configuration."
the acronyms: need to add more like "LCO, NMC, LFP, OCV, P2D, SPM, UKF, ...etc.
Experiment title change it to "Experimental Evaluation" since it also contains the metrics used and some analysis
Also the number of reference is too narrow, try to add more references to some claims you did (30 references sounds ok but not less than that), also use one consistent IEEE-style bibliography generated by BibTeX/Biber


1. 我把正文相关描述删掉了，表格上的就先留着。等博士生给我提供数据再改

2. 数据确认过了没问题。

3. nbd的最大capacity是1.8565，换算就是mae/1.8565

4. 可以，把涉及到的地方都展示出来。

5. ok

6. 好

7. 好

8. 好

9. 你把要增加的参考文献列出来，我核实真实性后再加


---

# 详细改动方案（待确认）

## ② 表 4.1 标签行改名

行 "SoH label range" → "Label range"
- NBD 格：`1.29--1.86\,Ah` → `1.29--1.86\,Ah (capacity)`
- NRD 格：`41.6--100\,\%` → `41.6--100\,\% (SoH)`
- 正文 Label units 段补充一句：NBD 的 SoH 以实测放电容量表达（NASA 数据集惯例），NRD 直接给百分比。

## ③ 表 4.4 重构

表头改为 8 列：`Dataset | WL | Model | MAE [%] | MSE [%] | Cycles [kcycles] | RAM [%] | Flash [%]`

**MAE [%]（已算好，NBD 归一化基准 = 最大容量 1.8565 Ah）：**

| wlen | LightGBM | BiLSTM |
|------|----------|--------|
| 20 | 1.013 | 1.045 |
| 30 | 0.840 | 0.873 |
| 40 | 0.738 | 2.160 |
| 50 | 0.921 | 1.551 |
| 60 | 0.932 | 1.514 |

NRD 的 MAE 本来已是 %，原值不动（2.2561 等）。

**Cycles [kcycles]（去 k/M 后缀）：**
- LightGBM：72.6 / 74.7 / 77.1 / 79.0 / 81.3（NBD）；73.3 / 75.4 / 77.8 / 79.6 / 82.0（NRD）
- BiLSTM：838 / 1250 / 1670 / 2080 / 2500（两数据集相同）

**❓ 需要你确认：**
1. MSE [%] 的公式？两个候选：
   - (a) RMSE 归一化：sqrt(MSE)/1.8565×100（NBD w20 L: 1.524）
   - (b) MSE/1.8565²×100（NBD w20 L: 0.023）——数值太小，不建议
   - NRD 的 MSE 单位是 %²，同法处理？
2. 原来的 MAE/MSE 列（Ah 单位）删掉还是保留？（8列 vs 10列）建议删，8列。
3. 表头用 "Flash [%]" 还是博士生写的 "Total Flash [%]"？注意 Flash 只算模型参数、不含固件，写 Total 会误导。建议 "Flash (model) [%]" 或 "Flash [%]"。
4. 正文 4.4.1 引用的 MAE 数值（0.0188 等）要不要同步改为 % 值？

我的确认：
1. 不确定
2. 删掉
3. Flash [%]
4. 改

## ④ 变长输入表述修正（12 处）

**原则**：LSTM 架构本身支持变长序列；限制来自部署的 BiLSTM 基线及其 SPC5Studio AI 转换流程使用固定形状输入张量。输入长度灵活性来自特征提取，任何 ML 模型都能受益。

1. **introduction.tex:80** — "deep learning architectures such as LSTMs cannot be dynamically resized at inference time"
   → "deep learning architectures such as LSTMs can only be resized by retraining, and the SPC5Studio AI conversion flow used for the deployed BiLSTM baseline requires a fixed input tensor shape"

2. **introduction.tex:92** — "Because the model operates on a fixed-size feature vector rather than on a raw temporal sequence, a single ensemble can process windows of arbitrary length without modification."
   → "Because the model operates on a fixed-size feature vector produced by feature extraction --- rather than on a raw temporal sequence --- a single model can process windows of arbitrary length without modification."

3. **background.tex:115** — "No support for variable-length inputs. When a real-world driving segment produces a window shorter or longer than the model expects, the only options --- truncation or zero-padding --- degrade accuracy."
   → "Fixed tensor shape in deployed toolchains. LSTM architectures support variable-length sequences in principle, but the SPC5Studio AI conversion used for the deployed baseline fixes the input tensor shape at deployment time. When a driving segment produces a window shorter or longer than expected, the only options are truncation or zero-padding, which degrade accuracy."

4. **background.tex:116** — "a BiLSTM trained on fixed-length windows can produce wildly fluctuating errors..."（表述基本准确，保留，仅微调）

5. **background.tex:130** — "Natural input-length independence. A decision tree model takes a fixed-size feature vector as input, not a raw time series... This is the property that makes the multi-length training strategy of this thesis possible."
   → 明确归功于特征提取："Natural input-length independence through feature extraction. Any model that consumes a fixed-size feature vector --- whether a decision tree or a neural network --- becomes independent of the raw window length, because the feature extraction step always outputs a 10-dimensional vector. This property, not any specific property of LightGBM, is what makes the multi-length training strategy possible."

6. **background.tex:164** — "it is the only method that simultaneously achieves high accuracy and freedom from fixed-length inputs"
   → "the combination of feature extraction with LightGBM is the only approach in the table that simultaneously achieves high accuracy and freedom from fixed-length inputs"

7. **methodology.tex:90** — "The BiLSTM was trained with 20-sample windows; it cannot process a 40-sample or 60-sample window without architectural changes."
   → "The BiLSTM was trained with 20-sample windows; its deployed implementation fixes the input tensor shape, so a 40-sample or 60-sample window cannot be handled without retraining."

8. **methodology.tex:232** — "Because a BiLSTM's input dimension is fixed at training time, covering five window lengths requires training five separate BiLSTM models."
   → "Because the deployed BiLSTM's input tensor shape is fixed by the SPC5Studio AI conversion, covering five window lengths requires training five separate BiLSTM models."

9. **methodology.tex:360** — "For the BiLSTM baseline, which cannot process variable-length inputs, unseen window lengths are accommodated..."
   → "For the BiLSTM baseline, whose deployed conversion fixes the input tensor shape, unseen window lengths are accommodated..."

10. **experiment.tex:81** — "since embedded hardware does not support variable-length inputs for BiLSTMs"
    → "since the SPC5Studio AI conversion used for the deployed BiLSTM fixes the input tensor shape"

11. **experiment.tex:295** — "recurrent networks processing fixed-length sequences"（基本准确，保留）

12. **experiment.tex:299** — "BiLSTM under the same conditions produces unpredictable and often severely degraded accuracy"（准确，保留）

## ⑤ 能耗标注 "Estimated"

1. **experiment.tex:123** 表头：`Energy [$\mu$J]` → `Estimated energy [$\mu$J]`
2. **experiment.tex:135**："Energy per inference drops from 279.36 μJ to 24.18 μJ" → "The estimated energy per inference drops from 279.36 μJ to 24.18 μJ"
3. **experiment.tex:115**："the table reports inference latency and energy per inference" → "...inference latency and estimated energy per inference"
4. conclusion 若提到 energy 检查（当前没有，无需改）

## ⑥ Summary 结尾重写

原："These results show that a single lightweight tree model can support reliable SoH monitoring on production automotive hardware, with little need for updates."

改（按博士生原意）：

"These results demonstrate the feasibility of lightweight SoH inference on an automotive-grade microcontroller. They also show that the approach reduces sensitivity to observation-window length, potentially lowering the need for retraining caused solely by changes in input-window configuration."

## ⑦ 缩写表扩充（glossaries.tex 新增 18 条）

LCO (Lithium Cobalt Oxide), NMC (Nickel Manganese Cobalt), LFP (Lithium Iron Phosphate), OCV (Open-Circuit Voltage), P2D (Pseudo-Two-Dimensional), SPM (Single Particle Model), UKF (Unscented Kalman Filter), LSTM (Long Short-Term Memory), BiLSTM (Bidirectional LSTM), CNN (Convolutional Neural Network), GBDT (Gradient-Boosted Decision Tree), SEI (Solid Electrolyte Interphase), EIS (Electrochemical Impedance Spectroscopy), RUL (Remaining Useful Life), CAN (Controller Area Network), TCU (Telematics Control Unit), AFE (Analog Front-End), HAL (Hardware Abstraction Layer)

要确认一下论文里有没有实际用到这些缩写

## ⑧ 章节名

"Experiment" → "Experimental Evaluation"（experiment.tex:1）

## ⑨ 参考文献（待你核实真实性）

拟新增 17 条（均已初步核实或为领域内经典文献）：

这些我要明天再确认，你先改其他的

1. Ke et al., "LightGBM: A Highly Efficient Gradient Boosting Decision Tree," NeurIPS, 2017
2. Akiba et al., "Optuna: A Next-generation Hyperparameter Optimization Framework," KDD, 2019
3. Hochreiter & Schmidhuber, "Long Short-Term Memory," Neural Computation 9(8), 1997
4. Doyle, Fuller, Newman, "Modeling of Galvanostatic Charge and Discharge of the Lithium/Polymer/Insertion Cell," J. Electrochem. Soc. 140(6), 1993
5. Peled & Menkin, "Review—SEI: Past, Present and Future," J. Electrochem. Soc. 164(7), 2017（已核实）
6. Waldmann, Hogg, Wohlfahrt-Mehrens, "Li plating as unwanted phenomenon in lithium-ion cells — A review," J. Power Sources 384, 2018
7. Fermín-Cueto et al., "Identification and machine learning prediction of knee-point and knee-onset in capacity degradation curves of lithium-ion cells," Energy and AI 1, 2020（已核实）
8. Severson et al., "Data-driven prediction of battery cycle life before capacity degradation," Nature Energy 4, 2019
9. Grieves, "Digital Twin: Manufacturing Excellence through Virtual Factory Replication," white paper, 2014
10. Tao et al., "Digital twin in industry: State-of-the-art," IEEE Trans. Industrial Informatics 15(4), 2019
11. UNECE Regulation No. 156 (software update), 2020
12. STMicroelectronics, SPC58EC80E5 datasheet DS11620
13. Plett, "Sigma-point Kalman filtering for battery management systems of LiPB-based HEV battery packs: Part 1," J. Power Sources 134, 2004
14. Santhanagopalan et al., "Review of models for predicting the cycling performance of lithium ion batteries," J. Power Sources 156, 2006
15. Ng et al., "Predicting the state of charge and health of batteries using data-driven machine learning," Nature Machine Intelligence 2, 2020
16. Hannan et al., "A review of lithium-ion battery state of charge estimation and management system in electric vehicle applications: Challenges and recommendations," Renewable and Sustainable Energy Reviews 78, 2017
17. Dubarry, Truchot, Liaw, "Synthesize battery degradation modes via a diagnostic and prognostic model," J. Power Sources 219, 2012

新增后总数 14+17=31。每条将挂到正文相应论断处。
同时 biblatex 风格改 IEEE：common/packages.tex 中 `style=numeric,citestyle=numeric` → `style=ieee`（需要你授权动 common/）。

## ⑩ 附带修复（我的额外发现）

background.tex:224 还有一处漏网："Performance profiling data ... are obtained from the SPC5Studio linker output (map file) and UDE cycle counters."——与已修正的 STMD3/.o 文件口径矛盾。改为："Cycle counts are measured with the MCU's system timer (STMD3), and Flash/RAM usage is derived from the compiled object files, as described in Chapter 3."

---

# 待办（下次继续）

## 1. 文献引用（博士生建议 ⑨）

- [ ] 核实拟新增 17 条文献的真实性（清单见上文 ⑨ 节）
- [ ] 核实后加入 bibliography.bib 并挂到正文相应论断处（目标总数 ≥30）
- [ ] 授权后把 biblatex 风格改为 IEEE：common/packages.tex 中 `style=numeric,citestyle=numeric` → `style=ieee`
- 注意：biber 有 9 个 "comma(s) at end of name" 警告（双花括号包裹的 author 字段，如 `{{Plett, Gregory}}`），改风格时可顺手清理

## 2. 表 4.5 泛化实验数据（数据问题 ①）

- [ ] BiLSTM₆₀ 在 NBD 上 wlen={10,35,55,75} 的真实 MAE（等博士生给数据）
- [ ] 当前表 4.5 中 BiLSTM₆₀ 行与 LightGBM 行完全相同，疑似错误（论文发表版也有此问题）
- [ ] 拿到数据后更新表 4.5 + 检查正文描述
- [ ] NBD 数据集的 MAE 是否也要改（与 SoH 尺度换算一致？）——待确认具体改法

## 其他备忘

- latexmk 依赖库损坏过：编译用 `pdflatex --shell-escape -interaction=nonstopmode -recorder -output-directory=build thesis.tex` ×2 + `biber build/thesis`，或先 `rm -rf build` 再 `latexmk -pdf`
- 封面 adjustbox 已移除、supervisors 列改为 p{\textwidth} 自动换行
- 表 4.4 的 MSE [%²] 换算方案已确认（NBD ÷1.8565²×100²，NRD 原值）
